# Docker Environment Configuration
# Used by docker-compose for service discovery

# API Gateway URL
VITE_API_URL=http://nginx:80

# WebSocket URL
VITE_WS_URL=ws://nginx:80/ws

# Feature flags
VITE_ENABLE_WEBSOCKET=true
VITE_ENABLE_METRICS=true
VITE_ENABLE_TRACING=true
